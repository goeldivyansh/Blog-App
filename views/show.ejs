<%- include("partials/header") %>
<!-- <%= blog %> -->

<div class="container d-flex justify-content-center">
    <div class="card mt-4 text-center" style="width: 60%;">
        <div class="card-body ">
            <h2> <%= blog.title %> </h2>
            <div class="mt-4">
                <img src="<%= blog.image %>" alt="image" height="300px" width="90%" style="border: solid 2px #f0f1f2;">
            </div>
            <div class="d-flex justify-content-end mr-4"><%= blog.created.toDateString() %></div>
            <p class="mt-3"><%= blog.body %></p>
        </div>

<!-- 
        <div class="flex-row  flex-row bd-highlight  mb-3">
            <div class="p-2 bd-highlight">
                <button type="button" class="btn btn-outline-info" style="width: 100px;">Info</button>
            </div>

            <div class="p-2 bd-highlight">
                <form action="/blogs/<%= blog._id %>?_method=DELETE" method="POST" style="display: inline;">
                    <button type="submit" class="btn btn-outline-danger mb-4">Delete</button>
                </form>
            </div>
        </div> -->
        <a href="/blogs/<%= blog._id%>/edit" class="btn btn-outline-info d-flex justify-content-center mb-2" style="width: 80%; margin: 0 auto;">Edit Blog</a>
        <form action="/blogs/<%= blog._id %>?_method=DELETE" method="POST" style="display: inline;">
            <button type="submit" class="btn btn-outline-danger mb-4" style="width: 80%;">Delete Blog</button>
        </form>
       
   
    </div>
</div>


<%- include("partials/footer") %>